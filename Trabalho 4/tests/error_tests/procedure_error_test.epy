procedure bar(atom n, integer m, object o, sequence s)
	integer n --ERROR (variavel n já está declarada nesse nivel)
	z = 0  --ERROR (z não foi declarado)
	o = n*e --ERROR (e não foi declarado)
end procedure

atom n,o
integer m, s

bar(n, m) --ERROR (param num)
