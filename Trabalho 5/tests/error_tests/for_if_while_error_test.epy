for i = a to b do -- ERRO (a e b nao declarados)
	integer d
	while c > i do
		integer z
		d = c
		if a < c then
			integer x,z --ERRO (z ja declarado)
			d = 1
		else
			x = 2 --ERRO (x nao delcarado)
 		end if
		integer x
	end while
	z = 1 --ERRO (z nao declarado)
end for
d = 1 --ERRO(d nao declarado)
