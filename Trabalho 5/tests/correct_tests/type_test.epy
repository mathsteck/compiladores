type hour(integer x)
    if x < 0 then
        return 0
    elsif x > 60 then
        return 0
    end if
    return 1
end type

hour h1, h2
h1 = 10 + h2

if h1 > h2 then
    hour h3
    h3 = h1 + h2
end if
